# ============================================================
# COPAS - Variables de Entorno
# Copia este archivo como .env y rellena los valores reales
# ============================================================

# --- SEGURIDAD WEBHOOK ---
# Token secreto que Make enviará en el header X-Webhook-Token
# Genera uno aquí: python -c "import secrets; print(secrets.token_hex(32))"
WEBHOOK_SECRET_TOKEN=cambia_este_valor_por_algo_secreto

# Puerto del servidor (Railway lo sobreescribe automáticamente)
PORT=8000

# --- SUPABASE ---
# URL de tu proyecto Supabase (ej: https://xxxx.supabase.co)
SUPABASE_URL=https://tu_proyecto.supabase.co
# Service Key (con permisos completos) - NO la anon key
SUPABASE_SERVICE_KEY=eyJxxx...

# --- WHATSAPP META CLOUD API ---
# Lo obtienes en: developers.facebook.com → tu App → WhatsApp → Getting Started
WHATSAPP_PHONE_NUMBER_ID=tu_phone_number_id
WHATSAPP_ACCESS_TOKEN=EAAxxxxx...  # Token permanente del Business

# Nombre exacto de tu plantilla aprobada en Meta
WHATSAPP_TEMPLATE_NAME=order_confirmation
WHATSAPP_TEMPLATE_LANGUAGE=es

# --- FRONTEND ---
# URL del backend (para desarrollo local)
VITE_API_URL=http://localhost:8000

# CORS - Orígenes permitidos (separados por coma)
CORS_ORIGINS=http://localhost:5173,https://tu-frontend.vercel.app
