{
    "name": "copas-root",
    "private": true,
    "scripts": {
        "build": "cd frontend && rm -f package-lock.json && npm install --no-audit && npm run build && cd .. && rm -rf public && mkdir -p public && cp -r frontend/dist/. public/"
    },
    "engines": {
        "node": "18.x"
    }
}